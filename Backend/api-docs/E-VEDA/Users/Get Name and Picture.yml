info:
  name: Get Name and Picture
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/user/picture-name"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
