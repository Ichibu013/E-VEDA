# 'postgres-db' only works if you have a service named postgres-db in your docker-compose.yml
DATABASE_URL=postgres://postgres:root@host.docker.internal:5432/e_veda?sslmode=disable

# 'minio' is the name of the container running MinIO
MINIO_ENDPOINT=minio:9000

GEMINI_API_KEY=<your_api_key>
